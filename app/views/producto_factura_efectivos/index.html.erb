<h1>Listing producto_factura_efectivos</h1>

<table>
  <tr>
    <th>Producto</th>
    <th>Factura</th>
    <th>Cantidad</th>
    <th>Precio</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @producto_factura_efectivos.each do |producto_factura_efectivo| %>
  <tr>
    <td><%= producto_factura_efectivo.producto_id %></td>
    <td><%= producto_factura_efectivo.factura_efectivo_id %></td>
    <td><%= producto_factura_efectivo.cantidad %></td>
    <td><%= producto_factura_efectivo.precio %></td>
    <td><%= link_to 'Show', producto_factura_efectivo %></td>
    <td><%= link_to 'Edit', edit_producto_factura_efectivo_path(producto_factura_efectivo) %></td>
    <td><%= link_to 'Destroy', producto_factura_efectivo, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Producto factura efectivo', new_producto_factura_efectivo_path %>
