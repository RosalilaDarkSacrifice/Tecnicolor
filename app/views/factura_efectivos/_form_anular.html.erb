<%= form_for(@factura_efectivo) do |f| %>
  <% if @factura_efectivo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@factura_efectivo.errors.count, "error") %> prohibited this factura_efectivo from being saved:</h2>

      <ul>
      <% @factura_efectivo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :anulada %><br />
    <%= f.check_box :anulada %>
  </div>
  <div class="actions">
    <%= f.submit "Finalizar"%>
  </div>
<% end %>
